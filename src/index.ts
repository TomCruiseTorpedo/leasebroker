// Scaffold placeholder. Bead `leasebroker-001` (contract) replaces this with the
// real barrel re-exporting `src/contract` (types + interfaces + zod schemas).
// Kept so `tsc --noEmit` and `vitest` have a valid baseline and stay green from
// the first commit — the fleet inherits a working feedback loop, not a red one.
export {};
